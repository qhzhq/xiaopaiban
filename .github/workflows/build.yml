name: Build All Platforms

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      version:
        description: 'Version to build (e.g., 1.5.1)'
        required: true
        default: '1.5.1'

env:
  APP_VERSION: ${{ github.event.inputs.version || '1.5.1' }}

jobs:
  build-macos:
    runs-on: ${{ matrix.runner }}
    strategy:
      matrix:
        arch: [x86_64, aarch64]
        include:
          - arch: x86_64
            runner: macos-13
            rust-target: x86_64-apple-darwin
          - arch: aarch64
            runner: macos-latest
            rust-target: aarch64-apple-darwin
    steps:
      - uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          
      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.rust-target }}
          
      - name: Install Pake CLI
        run: npm install -g pake-cli
        
      - name: Prepare build files
        run: |
          mkdir -p pake-build
          cp index.html main.js favicon.ico icon.png icon.icns pake-build/ 2>/dev/null
          cp pake.json pake-build/
          cp -r Images pake-build/
          cp -r js pake-build/
          
      - name: Build macOS DMG (${{ matrix.arch }})
        working-directory: pake-build
        run: pake --config pake.json --targets dmg
          
      - name: Upload macOS DMG (${{ matrix.arch }})
        uses: actions/upload-artifact@v4
        with:
          name: macos-${{ matrix.arch }}-dmg
          path: |
            pake-build/target/release/bundle/dmg/*.dmg

  build-windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          
      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: x86_64-pc-windows-msvc
          
      - name: Install Pake CLI
        run: npm install -g pake-cli
        
      - name: Prepare build files
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force -Path pake-build
          Copy-Item index.html, main.js, favicon.ico, icon.png, pake.json -Destination pake-build
          Copy-Item -Recurse Images -Destination pake-build\Images
          Copy-Item -Recurse js -Destination pake-build\js
          
      - name: Build Windows EXE
        working-directory: pake-build
        run: pake --config pake.json --targets nsis
          
      - name: Build Windows MSIX
        working-directory: pake-build
        run: pake --config pake.json --targets msix
          
      - name: Upload Windows EXE
        uses: actions/upload-artifact@v4
        with:
          name: windows-x86_64-exe
          path: |
            pake-build/target/release/bundle/nsis/*.exe
            
      - name: Upload Windows MSIX
        uses: actions/upload-artifact@v4
        with:
          name: windows-x86_64-msix
          path: |
            pake-build/target/release/bundle/msix/*.msix
            pake-build/target/release/bundle/msix/*.msixupload

  build-linux:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          
      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: x86_64-unknown-linux-gnu
          
      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libgtk-3-dev \
            libwebkit2gtk-4.1-dev \
            libappindicator3-dev \
            librsvg2-dev \
            patchelf
          
      - name: Install Pake CLI
        run: npm install -g pake-cli
        
      - name: Prepare build files
        run: |
          mkdir -p pake-build
          cp index.html main.js favicon.ico icon.png icon.icns pake.json pake-build/
          cp -r Images pake-build/
          cp -r js pake-build/
          
      - name: Build Linux DEB
        working-directory: pake-build
        run: pake --config pake.json --targets deb
          
      - name: Upload Linux DEB
        uses: actions/upload-artifact@v4
        with:
          name: linux-x86_64-deb
          path: |
            pake-build/target/release/bundle/deb/*.deb

  create-release:
    needs: [build-macos, build-windows, build-linux]
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/')
    permissions:
      contents: write
    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts
          
      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          files: |
            artifacts/macos-x86_64-dmg/*.dmg
            artifacts/macos-aarch64-dmg/*.dmg
            artifacts/windows-x86_64-exe/*.exe
            artifacts/windows-x86_64-msix/*.msix
            artifacts/windows-x86_64-msix/*.msixupload
            artifacts/linux-x86_64-deb/*.deb
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
